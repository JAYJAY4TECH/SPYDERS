const router = require('express').Router();
router.post('/initialize', (req, res) => res.status(501).json({ error: 'Not implemented yet' }));
router.get('/verify/:ref', (req, res) => res.status(501).json({ error: 'Not implemented yet' }));
module.exports = router;
